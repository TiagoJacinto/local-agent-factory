import { SSSFConfig } from "./data_types"; import { Tracer } from "./tracer"; import { Run } from "./runner"; import { engineerName, newId, nowIso } from "./utils"; import { basename } from "node:path";
export function ensure(cfg:SSSFConfig,adwId?:string){const id=adwId||newId(8),tracer=new Tracer(cfg.observability.db,`${cfg.defaults.data_dir}/sessions/${id}/events.jsonl`),run=new Run(cfg,id,tracer,engineerName());tracer.sessionStart(id,run.engineer,basename(process.argv[1]||"adw"));tracer.processStart(id,"adw","",process.pid,process.argv.join(" "));process.once("SIGTERM",()=>{tracer.sessionFinish(id,false);process.exit(128+15)});process.once("SIGINT",()=>{tracer.sessionFinish(id,false);process.exit(128+2)});run.console.sessionStarted(id,run.engineer);return run;}
