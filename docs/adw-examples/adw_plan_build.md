# `adw_plan_build`

Use for small, well-understood work.

Chain: **request → planner → builder → git commit → review**.

The planner creates an implementation plan. The builder implements it. A code
phase commits the result, then the run waits for review.
