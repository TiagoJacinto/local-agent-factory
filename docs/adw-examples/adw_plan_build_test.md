# `adw_plan_build_test`

Use for the standard planned implementation flow.

Chain: **request → planner → builder → test/fix loop → git commit → review**.

This combines planning, implementation, deterministic testing, bounded repair,
and a final commit.
