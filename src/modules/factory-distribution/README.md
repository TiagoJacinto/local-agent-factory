# Factory distribution

Public entrypoints are the application scripts under `application/`: build/check the skill, package it, install it, and generate configuration or workflow assets. Generated package output is written to `dist/` and verified with `bun run check:skill`.
